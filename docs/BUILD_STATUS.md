# Build Status

Eagle Eye is built and tested through the repository's Windows GitHub Actions workflow.

The workflow validates the Python test suite, creates a portable Windows x64 application with PyInstaller, and generates an Inno Setup installer.
