# Windows Build Verification

This file records the first pull-request validation of the Eagle Eye Windows application.

The repository workflow runs the automated test suite, builds the portable x64 application with PyInstaller, and creates the Windows installer with Inno Setup.
